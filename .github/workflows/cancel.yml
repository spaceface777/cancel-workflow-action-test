name: Cancel previous workflows

on:
  workflow_run:
    workflows: ["CI"]
    types:
      - requested

jobs:
  cancel-previous-workflows:
    runs-on: ubuntu-latest
    steps:
    - uses: spaceface777/cancel-workflow-action@master
      with:
        workflow_id: ${{ github.event.workflow.id }}
